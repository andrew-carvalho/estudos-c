// A biblioteca stdio.h contém funções úteis para imprimir e ler dados do
// usuário
#include <stdio.h>

// A função main que determina o ponto de entrada do programa
int main() {
  // A função printf imprime alguma string no stdout (saída padrão do sistema)
  printf("Hello world\n");
  // Todo programa em C deve retornar um código para o sistema (0 indica que o
  // programa executou sem problemas)
  return 0;
}
